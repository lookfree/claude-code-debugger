# Quick Start Guide

## 快速开始指南

### 1. 安装依赖

```bash
npm install
```

### 2. 开发模式运行

```bash
npm run electron:dev
```

这将启动：
- Vite 开发服务器 (端口 5173)
- Electron 应用窗口（带热重载）

### 3. 项目结构说明

```
skills-ui/
├── electron/              # Electron 主进程
│   ├── main.ts           # 主入口
│   ├── preload.ts        # 预加载脚本（IPC 桥接）
│   ├── services/         # 核心服务
│   │   └── file-manager.ts  # 文件系统管理器
│   └── ipc/              # IPC 处理器
│       ├── skills.ts     # Skills 相关 IPC
│       ├── agents.ts     # Agents 相关 IPC
│       ├── hooks.ts      # Hooks 相关 IPC
│       ├── mcp.ts        # MCP 相关 IPC
│       └── commands.ts   # Commands 相关 IPC
├── src/                  # React 前端
│   ├── components/       # UI 组件
│   │   ├── ui/          # shadcn/ui 基础组件
│   │   └── layout/      # 布局组件
│   ├── pages/           # 页面组件
│   │   ├── Dashboard.tsx    # 仪表板
│   │   ├── Skills.tsx       # Skills 浏览器
│   │   ├── Agents.tsx       # Agents 管理器
│   │   ├── Hooks.tsx        # Hooks 管理器
│   │   ├── MCP.tsx          # MCP 服务器管理
│   │   ├── Commands.tsx     # 命令管理器
│   │   ├── Graph.tsx        # 依赖图谱
│   │   └── Settings.tsx     # 设置
│   ├── lib/             # 工具库
│   │   ├── api.ts       # IPC API 封装
│   │   └── utils.ts     # 工具函数
│   ├── App.tsx          # 主应用组件
│   ├── main.tsx         # 前端入口
│   └── index.css        # 全局样式
├── shared/              # 共享代码
│   └── types/           # TypeScript 类型定义
│       ├── skill.ts
│       ├── agent.ts
│       ├── hook.ts
│       ├── mcp.ts
│       └── command.ts
└── package.json
```

### 4. 核心功能

#### 已实现
- ✅ 项目基础架构（Electron + React + TypeScript）
- ✅ 文件系统管理器（读取 .claude/ 配置）
- ✅ IPC 通信层（主进程 ↔ 渲染进程）
- ✅ Dashboard 仪表板
- ✅ Skills 浏览器（参考截图实现）
  - Skills 列表展示
  - 详细信息面板
  - 搜索和过滤
  - 统计信息展示

#### 待实现
- ⏳ Agents 管理器
- ⏳ Hooks 管理器
- ⏳ MCP 服务器管理和连接测试
- ⏳ Commands 管理器
- ⏳ 依赖关系图谱（React Flow）
- ⏳ 可视化编辑器
- ⏳ 实时调试功能
- ⏳ 性能分析
- ⏳ AI 辅助配置

### 5. 开发流程

1. **修改代码**：
   - 前端：修改 `src/` 下的文件，Vite 会自动热重载
   - 后端：修改 `electron/` 下的文件，需要重启 Electron

2. **添加新页面**：
   - 在 `src/pages/` 创建新组件
   - 在 `src/App.tsx` 添加路由
   - 在 `src/components/layout/Layout.tsx` 添加导航

3. **添加新的 IPC 处理器**：
   - 在 `electron/ipc/` 创建新的处理器文件
   - 在 `electron/ipc/index.ts` 注册
   - 在 `electron/preload.ts` 暴露 API
   - 在 `src/lib/api.ts` 封装调用

4. **添加新类型**：
   - 在 `shared/types/` 定义共享类型
   - 在 `shared/types/index.ts` 导出

### 6. 构建和打包

```bash
# 构建生产版本
npm run electron:build
```

生成的安装包在 `release/` 目录：
- macOS: `.dmg` 和 `.zip`
- Windows: `.exe` 安装程序和 `.zip`
- Linux: `.AppImage` 和 `.deb`

### 7. 调试技巧

1. **前端调试**：
   - 开发模式下自动打开 DevTools
   - 使用 React DevTools 扩展

2. **主进程调试**：
   - 在 `electron/main.ts` 中使用 `console.log`
   - 查看终端输出

3. **IPC 调试**：
   - 在 IPC 处理器中添加日志
   - 在前端使用 `console.log` 查看返回值

### 8. 常见问题

**Q: 启动失败，提示端口占用**
A: 检查 5173 端口是否被占用，修改 `vite.config.ts` 中的端口号

**Q: 依赖安装失败**
A: 尝试清除缓存：`rm -rf node_modules package-lock.json && npm install`

**Q: Electron 窗口空白**
A: 检查浏览器控制台的错误信息，通常是路径或导入问题

**Q: 无法读取 .claude/ 配置**
A: 确保项目根目录或 ~/.claude/ 存在配置文件

### 9. 下一步计划

1. 实现 Agents 管理器页面
2. 实现 Hooks 管理器页面
3. 实现 MCP 服务器管理和连接测试
4. 添加依赖关系图谱可视化
5. 添加可视化配置编辑器
6. 集成 Monaco Editor 用于代码编辑
7. 添加性能监控和日志查看
8. 实现配置验证和错误提示

### 10. 贡献指南

欢迎贡献代码！请遵循以下步骤：

1. Fork 项目
2. 创建特性分支：`git checkout -b feature/new-feature`
3. 提交更改：`git commit -m "Add new feature"`
4. 推送分支：`git push origin feature/new-feature`
5. 创建 Pull Request

### 技术支持

如有问题，请提交 Issue 或联系项目维护者。

---

**祝开发愉快！🎉**
